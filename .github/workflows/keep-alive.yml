name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Revenue AI Copilot
        run: |
          curl -L --fail --retry 3 "https://revenue-ai-copilot-2pgrnlgyantu75qnaet59x.streamlit.app"
